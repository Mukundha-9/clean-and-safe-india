services:
  - type: web
    name: clean-and-safe-india
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
